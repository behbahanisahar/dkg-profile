import React from "react";

const Chat: React.FC = () => {
  return (
    <div>
      {/* <AppContextConsumer>
        {changeText => (
          <>
            <input
              id="username"
              type="text"
              onChange={event => {
                changeText.changeText(event.target.value);
              }}
            />
            <p>{changeText.text}</p>
          </>
        )}
      </AppContextConsumer> */}
      test
    </div>
  );
};

export default Chat;
